Equipment Rental System console app for renting equipment like laptops projectors cameras supports users rentals returns availability and reports run with dotnet run structure is Models for data Services for logic, Program for run design keeps logic out of models and Program - so code is simpler services control rules and make them easy to change cohesion. Each class has one job - coupling is low classes are not strongly connected decisions like limits and penalties are in, so they are easy to change
